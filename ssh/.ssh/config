# Safe to publish: paths and options only, no keys or secrets.
# UseKeychain is macOS-only; IgnoreUnknown keeps Linux's OpenSSH from
# rejecting it instead of just skipping it.
Host *
  IgnoreUnknown UseKeychain
  AddKeysToAgent yes
  UseKeychain yes
  IdentityFile ~/.ssh/id_ed25519
